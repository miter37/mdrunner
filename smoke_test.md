# Smoke Test

Read the top 5 headlines from `https://finviz.com/news` and print each one as a bullet point.

Do not summarize. Do not explain. Just print the 5 headlines verbatim as they appear on the page.

End with the line `Saved: /tmp/mdrunner_smoke_<timestamp>.md` where `<timestamp>` is the current UTC time in `YYYYMMDD_HHMMSS` format.
